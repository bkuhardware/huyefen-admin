import React from 'react';
import PageHeader from '@/components/PageHeader';

const Dashboard = () => {
    return (
        <PageHeader>
            <div>Dashboard</div>
        </PageHeader>
    )
};

export default Dashboard;